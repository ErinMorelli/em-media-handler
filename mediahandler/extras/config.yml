General:
    keep_files: no
    keep_if_skips: yes

Deluge:
    enabled: no
    ssl: no
    host: 127.0.0.1
    port: 8112
    password:
    endpoint: /json

Logging:
    enabled: yes
    level: 30
    log_file:

Notifications:
    enabled: no
    notify_name:
    pushover:
        api_key:
        user_key:
    pushbullet:
        token:

TV:
    enabled: yes
    folder:
    ignore_subs: yes
    format: '{n}/Season {s}/{n.space(".")}.{"S"+s.pad(2)}E{e.pad(2)}'
    log_file:
    filebot:

Movies:
    enabled: yes
    folder:
    ignore_subs: yes
    format: '{n} ({y})'
    log_file:
    filebot:

Music:
    enabled: no
    log_file:
    beets:

Audiobooks:
    enabled: no
    folder:
    api_key:
    make_chapters: off
    chapter_length: 8
    abc:
    php:
